import practice from '@common/practice';
import daily from '@common/daily';
import idiom from '@common/idiom';
import proverb from '@common/proverb';

const data: Record<string, string[]> = { practice, daily, idiom, proverb };

export default data;
